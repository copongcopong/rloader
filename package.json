{
  "name": "rloader",
  "version": "0.1.0",
  "description": "express router loader",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "require-directory": "^2.1.0"
  }
}
